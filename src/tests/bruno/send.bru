meta {
  name: send
  type: http
  seq: 5
}

post {
  url: {{baseurl}}/{{version}}/send
  body: json
  auth: inherit
}

body:json {
  {
    "consumer_key": "TODO",
    "access_token": "wrong_token_sorry",
    "actions": [
    ]
  }
}

settings {
  encodeUrl: true
}
